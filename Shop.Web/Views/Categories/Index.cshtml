@model IEnumerable<Shop.Web.ViewModels.CategoryViewModel>

@{
    ViewData["Title"] = "Categories";
}


@foreach (var item in Model)
{
    <div>
        @Html.DisplayFor(modelItem => item.Name)
    </div>
}
